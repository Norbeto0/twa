<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌀✨ Enchanted Login Realm ✨🌀</title>
    <script>
        async function sendReq(url, method, headers = null, data = null) {
            const options = { method: method, headers: { "Content-type": "application/json", ...headers } }
            if (method != "GET" && data != null) options.body = JSON.stringify(data);
            const res = await fetch(url, options)
            return res;
        }

        async function loginHandle(e) {
            e.preventDefault()
            const username = document.getElementById("username").value
            const password = document.getElementById("password").value
            const result = await sendReq('http://localhost:3000/auth/login', "POST", null, { username, password });
            window.location.assign('/administration')
        }
    </script>

    <style>
        body {
            font-family: 'Comic Sans MS', cursive;
            background: url('https://media1.tenor.com/m/xQ24GQOIYZAAAAAd/space.gif');
            background-size: cover;
            background-blend-mode: multiply;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden; /* Hide overflow to prevent scrollbars */
        }

        form {
            background: rgba(255, 255, 255, 0.8);
            padding: 2rem;
            border-radius: 50%;
            box-shadow: 0 0 50px rgba(255, 255, 255, 0.8);
            display: flex;
            flex-direction: column;
            gap: 1rem;
            border: 5px dashed #ff69b4;
            animation: moveForm 8s infinite linear; /* Apply the animation */
        }

        @keyframes moveForm {
            0% {
                transform: rotate(0deg) translate(200px) rotate(0deg);
            }
            100% {
                transform: rotate(360deg) translate(200px) rotate(-360deg);
            }
        }

        input[type=text],
        input[type=password] {
            padding: 1.5rem;
            border: 3px solid #ff69b4;
            border-radius: 25px;
            font-size: 1.8rem;
            outline-color: #ff69b4;
            color: #4b0082;
            font-weight: bold;
            text-align: center;
        }

        input[type=submit] {
            padding: 2rem;
            border: none;
            border-radius: 30px;
            background: linear-gradient(135deg, #ff69b4, #ff1493);
            color: white;
            font-size: 2rem;
            cursor: pointer;
            transition: background 0.3s ease;
            font-weight: bold;
            text-transform: uppercase;
        }

        input[type=submit]:hover {
            background: linear-gradient(135deg, #ff1493, #c71585);
        }

        input[type=submit]::before {
            content: "🌈";
            position: relative;
            top: 5px;
            left: 5px;
            font-size: 50px;
        }
    </style>

</head>

<body>
    <form onsubmit="loginHandle(event)">
        <input type="text" id="username" placeholder="🌟 Enter your Enchanted Username">
        <input type="password" id="password" placeholder="🔮 Magical Passkey">
        <input type="submit" value="✨ Unleash the Sorcery">
    </form>
</body>

</html>
