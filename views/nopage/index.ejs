<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀🌌 Something Went Skibidi-Skibida! 🌌🚀</title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive;
            background-color: #c0ffee; /* Because why not? */
            text-align: center;
            padding-top: 100px;
            margin: 0;
            overflow: hidden; /* For added weirdness */
        }

        h1 {
            font-size: 8rem;
            color: #ff00ff;
            margin: 0;
            transform: rotate(-10deg);
            animation: shake 1s ease-in-out infinite alternate;
        }

        p {
            font-size: 3rem;
            color: #00ff00;
            margin-bottom: 50px;
            animation: glow 2s ease-in-out infinite alternate;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            padding: 20px 30px;
            background: linear-gradient(45deg, #ff4757, #00d2d3);
            color: #fff;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            font-size: 2rem;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        a:hover {
            transform: scale(1.1);
            background-color: #ff6b81;
        }

        @keyframes shake {
            0% {
                transform: rotate(-10deg);
            }

            100% {
                transform: rotate(10deg);
            }
        }

        @keyframes glow {
            0% {
                color: #00ff00;
                text-shadow: 0 0 10px #00ff00, 0 0 20px #00ff00, 0 0 30px #00ff00;
            }

            100% {
                color: #ff00ff;
                text-shadow: 0 0 10px #ff00ff, 0 0 20px #ff00ff, 0 0 30px #ff00ff;
            }
        }
    </style>
</head>

<body>
    <h1>404</h1>
    <p>Skibidi toilet is not here</p>
    <a href="/">Go back...</a>
</body>

</html>
